{
  "name": "@a0-events/events-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker which will talk to the Durable Object",
  "author": "Sandrino Di Mattia",
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler build",
    "publish": "npm run build && ./scripts/deploy.sh",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "@a0-events/common": "^1.0.0"
  }
}